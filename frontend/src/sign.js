class Sign{
    constructor(id, name, img_src, description){
        this.id = id;
        this.name = name;
        this.img_src = img_src;
        this.description = description;
    }

    // addSign(array) {
    //     array.push(this)
    // }
}